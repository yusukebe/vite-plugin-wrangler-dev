//@ts-ignore
document.addEventListener('DOMContentLoaded', () => {
  //@ts-ignore
  const root = document.getElementById('root') as HTMLElement
  if (root) {
    root.textContent = 'Hello from client.ts!!'
  }
})
